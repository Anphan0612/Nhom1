# Nhom1

## Frontend (React/Vite)
To run the React frontend:
1. Navigate to the `FluidConcierge` directory.
2. Run `npm install` (if you haven't installed dependencies yet).
3. Run `npm run dev`.

## Backend (Java/Spring Boot)
The backend is located in the `tripplanner` directory.
